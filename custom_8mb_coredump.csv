# Name,   Type, SubType, Offset,  Size, Flags
# FPVGateAIO diagnostic table: identical to custom_8mb.csv plus a coredump
# partition. The Arduino core already has CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH,
# but without this partition the panic handler has nowhere to write, so
# crashes leave no backtrace. Temporary - revert once the panic is fixed.
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x200000,
app1,     app,  ota_1,   0x210000,0x200000,
spiffs,   data, spiffs,  0x410000,0x100000,
coredump, data, coredump,0x510000,0x10000,
